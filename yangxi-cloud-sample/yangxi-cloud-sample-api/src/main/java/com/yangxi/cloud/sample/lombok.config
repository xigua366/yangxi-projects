## 消除＠Data注解编辑器warn警告
## 相当于实体类上配置了 @EqualsAndHashCode(callSuper=true) OR @ToString(callSuper = true)
## 此文件需要放在src/main/java文件夹下的目录中（也可以放在实体类同级目录下），其它位置无效
config.stopBubbling=true
lombok.equalsAndHashCode.callSuper=call
lombok.toString.callSuper=call