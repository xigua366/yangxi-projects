server:
  port: 8080

spring:
  profiles:
    active: dev
  application:
    name: yangxi-cloud-sample-service
  cloud:
    nacos:
      # 配置中心
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml